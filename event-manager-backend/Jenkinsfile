pipeline {
    agent { 
        node { 
            label 'built-in' 
        } 
    }

    stages {
        stage('Maven Build & Test') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}